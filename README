环境要求：
    nodejs支持、curl

功能：
    该脚本实现通过QQ空间自动登录并保存 cookie 里的 skey 以便在腾迅其他产品上实现各种 POST 或 GET。

使用：
    cd ${path}/auto_login_qq/
    ./auto_login.sh QQ号/邮箱/手机 QQ密码
    或
    ./auto_login.sh -c config.ini

注：
建议采用 ./auto_login.sh -c config.ini，此方法不会在 history 中留下痕迹
若在非私人机器上使用，为安全起见用完记得清除 config.ini 文件或者将内容里 QQ密码 清除掉。
